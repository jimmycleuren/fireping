{% extends 'base.html.twig' %}

{% block body %}
    <h1>Slavegroup</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ slaveGroup.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ slaveGroup.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('slavegroup_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('slavegroup_edit', { 'id': slaveGroup.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
