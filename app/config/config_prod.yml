imports:
    - { resource: config.yml }

#doctrine:
#    orm:
#        metadata_cache_driver: apc
#        result_cache_driver: apc
#        query_cache_driver: apc

monolog:
    handlers:
        main:
            type: stream
            path: '%kernel.logs_dir%/%kernel.environment%.log'
            level: info
        console:
            type: console

guzzle:
    logging: true

    clients:
        api_fireping:
            base_url: "https://smokeping-dev.cegeka.be"

            options:
                headers:
                    Accept: "application/json"